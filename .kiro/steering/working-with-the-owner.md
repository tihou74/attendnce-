---
inclusion: always
---

# التعامل مع مالك المشروع

## اللغة

يتواصل بالعربية — **أجِب بالعربية دائمًا**. رسائله قصيرة وقد تحتوي أخطاء إملائية أو تكتب بتخطيط لوحة مفاتيح خاطئ (مثل `gl htil adx`). عند الغموض في قرار بنيوي، اسأل سؤالاً واحدًا قصيرًا للتأكيد بدل التخمين، وصرّح بما افترضته.

## غير تقني — هذه أهم نقطة

**لا يستخدم سطر الأوامر.** تعليمات مثل `python -m http.server` أو `cd` أو أي أمر طرفية أوقفت التقدّم فعليًا في المحادثة («لم افهم»).

**أسلوبه:** يفتح رابط `raw.githubusercontent.com` في المتصفح ← `Ctrl+A` ← `Ctrl+C` ← يلصق في Notepad ← يحفظ.

لذلك:
- سلّم النتائج كـ **قائمة روابط raw**، رابط لكل ملف، بصيغة `https://raw.githubusercontent.com/{owner}/{repo}/{branch}/{path}`.
- ذكّره دائمًا بالحفظ بـ `Save as type = All Files` و `Encoding = UTF-8` حتى لا تتشوّه العربية.
- اقترح أيضًا رابط `github.com/.../blob/...` لأن فيه زر **Copy raw file** بضغطة واحدة.
- **فضّل حلاً بملف واحد مستقل** على حلٍّ يحتاج مجلدات أو خادمًا (لهذا وُلد `import-training-standalone.html`).
- إن قال إن رابطًا «لا يفتح»، السبب المعتاد **مسافة زائدة** نُسخت مع الرابط — تحقّق من الرابط بنفسك قبل أن تفترض خللاً في المستودع.

## الرفع إلى GitHub

قال في منتصف المشروع صريحًا **«لا ترفع على القيت هيب»**، ثم وافق لاحقًا على الرفع إلى مستودعات يملكها. **استأذن قبل الرفع**، إلا إن كان قد طلب روابط للتو — فالرفع حينها هو الخطوة الممكِّنة، وصرّح بذلك.

## سير عمل المحتوى

- يرسل **دليل قسم واحد في كل رسالة**، ويتوقّع تأكيدًا بما سُجِّل قبل الانتقال للتالي.
- التنسيق المختار صريحًا: **العربية في الأعلى والإنجليزية تحتها**.
- أحيانًا يرسل العربية فقط ويتوقّع أن تكتب الإنجليزية. وعندما يرسل الإنجليزية الرسمية من الإدارة (كما في دليل النظافة) **استخدم نصّه حرفيًا** لا ترجمتك.
- أشِر دائمًا إلى أسماء الأطباق العراقية/الخليجية المنقولة صوتيًا ليراجعها.
- إن ذكر رأس المستند البراندين، اسأل/أكّد أن الدليل مشترك ثم ولّد مستندين.

## توقّع الثقة

سأل صراحة: «هل فهمت كل شيء لكي لا تخرب عملي؟». لذلك:

- اذكر **بالتفصيل** ما تحقّقت منه فعليًا، وما تحقّقت منه جزئيًا، وما **لم** تستطع التحقق منه إطلاقًا.
- لا تبالغ في ادّعاء التحقق، ولا تقل «تم» قبل إثبات ذلك بمخرجات حقيقية.
- **لا تصلح قاعدة عمل بصمت** — اطرحها سؤالاً (انظر `open-issues.md`).
- إن أخطأت، صرّح بالخطأ وصحّحه (حدث فعلاً: طُبعت `OK` مضلّلة لأن `node` لم يكن في `PATH`).


## ⚠️⚠️⚠️ «نافذة كذا» — اسألْ **أيَّ** نافذة، ولا تُخمّن

وقع فعلًا وكلّف دورةً كاملة. قال المالك: «أيُّ بضاعةٍ وصلت وتمّ تسعيرُها تختفي
أوتوماتيكيًّا من **نافذة وصل حديثًا**». وفي النظام **شاشتان** بهذا الاسم تقريبًا:

| الشاشة | الاسمُ فيها | مَن يراها |
|---|---|---|
| `abaya-balances.html` | «Newly arrived / وصل حديثًا» | السيدة فاطمة |
| `admin.html` (تبويب) | «New goods arrived / بضاعة جديدة وصلت» | المالك |
| `abaya-seller.html` | «New goods arrived» (مهمّةُ الرفّ) | البياعات |

خمّنتُ أنها لوحةُ السيدة فاطمة، فأصلحتُ فيها عطلًا **حقيقيًّا** (مفتاحُ «أظهر
المُرسَلة» يبقى مفتوحًا بقيّةَ الجلسة)، ورفعتُ وأبلغتُ. ثمّ أرسل صورةَ شاشةٍ
فيها أعمدةُ **الدولار والمجموع وكوست القطعة والمورّد وأزرارُ Edit/Delete** — أي
جدولُ لوحته هو، ومُرشِّحُه كان يبدأ على «Everything».

**الدرس:** الأمرُ كان صحيحًا وفهمي ناقصًا. وكلفةُ السؤال جملةٌ واحدة، وكلفةُ
التخمين دورةٌ كاملةٌ وثقةٌ تُهتزّ. فحين يُسمّي شاشةً باسمٍ يحمله أكثرُ من موضع:
**اذكرِ المواضعَ واسألْ أيَّها يقصد** — أو أصلحْ **كلَّها** وقلْ إنك فعلت.

⚠️ وأسرعُ طريقٍ لمعرفة الشاشة من صورة: ابحث عن عمودٍ أو زرٍّ **لا يوجد إلّا
فيها**. أزرارُ Edit/Delete وأعمدةُ الشراء (USD · Total · Cost/piece · Supplier)
لا تظهر إلّا في لوحة المالك؛ ولوحةُ البياعة لا تحمل مالًا إطلاقًا.

## ⚠️ ولا تُبلّغ «تمّ» عن طلبٍ فيه أكثرُ من موضعٍ محتمل

قلتُ «تختفي أوتوماتيكيًّا — تمّ» وكانت تظهر أمامه. فالإبلاغُ عن إنجازٍ يجب أن
يُسمّي **الشاشةَ التي تحقّق فيها** بالاسم والمسار («لوحة الإدارة ← بضاعة جديدة
وصلت ← جدول Everything that arrived»)، لا بالوصف العامّ. فإن كانت الشاشةُ الخطأ
عرف المالكُ ذلك من سطرِ التقرير نفسِه لا بعد أسبوع.


## ⚠️ «مؤكَّدة» لا تعني أنّ بصمتَها تعمل — وسمٌ واحدٌ لقدرتَين هو كذبٌ بالصمت

سأل المالك: «لماذا الأتندس شيت لا يعمل بعد؟» فوجدتُ السببَ في **جدولي أنا**:

صفُّ البياعة كان يقول **`✔ confirmed`** لمجرّد وجود مستندِ بياعة. وشاشتُها تُسجّل
البصمةَ تحت حقل `employeeId` على ذلك المستند، **وبغيابه يكون زرّا الحضور والانصراف
مُعطَّلين**. فشاشتُها تقول لها الحقيقةَ، ولوحتُه تقول له «تمّ» — **فالخللُ كان في
لوحته وحدَها**، وهو يظنّ أنّ العطلَ عندها.

ومصدرُ مستندٍ بلا هذا الحقل: زرُّ **Add** في أسفل الجدول — يُنشئ بياعةً تدخل وتُرسل
التقريرَ اليوميَّ، بلا ملفِّ موظفةٍ وبلا بصمة.

**فصارت ثلاثَ حالاتٍ لا حالتَين** (01b85d5): مربوطة · غيرُ مربوطةٍ أصلًا · مربوطةٌ
بملفٍّ **آخر** (أي أنّ بصماتِها كانت تُكتب تحت اسم موظفةٍ أخرى). ومع الحالتين
الأخيرتين زرُّ `Link her attendance`، والثالثةُ تسأل بالاسمَين لأنّ البصماتِ
الماضيةَ **لا تُنقل**. ومعها تحذيرٌ إن كان لاسمٍ **مستندان** في `abaya_sellers`:
قائمةُ الأسماء في شاشتها تُبنى من المجموعة كلِّها، فيظهر اسمُها **مرّتين** بلا فرقٍ
ظاهرٍ وواحدةٌ منهما لا تبصم.

### القاعدةُ المستخلَصة، وتُعمَّم على كلِّ وسمِ حالة

**وسمٌ واحدٌ لا يُطلق على أكثرِ من قدرة.** «مؤكَّدة» كانت تعني ثلاثةَ أشياء: تدخل ·
تُرسل التقرير · تبصم. وسقوطُ الثالثة وحدها لم يكن له موضعٌ على الشاشة، فصار الوسمُ
يُخفي عطلًا بدل أن يُظهره. فمتى كان الوسمُ يغطّي قدراتٍ عدّة، **يُفحص كلُّ شرطٍ
ويُسمّى الساقطُ بعينه**.

⚠️ **وزرٌّ مخفيٌّ بحقٍّ يحتاج سطرًا يشرح متى يظهر.** سأل «أين أعيّنها؟» ولم يجد مفتاحَ
التوزيع — لأنني أخفيتُه على صفٍّ غيرِ مؤكَّدٍ (والعلَمُ يُكتب على مستندٍ لم يُخلق
بعد، فالإخفاءُ صحيح). **لكنّ السكوتَ خطأ**: صار الصفُّ يقول إنّ المفتاحَ يظهر بعد
التأكيد (c47f3ae). فالاختفاءُ الصحيحُ بلا تفسيرٍ يُقرأ عطلًا.
